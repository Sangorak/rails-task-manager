<div class="card">
<h1>Here are the details</h1>
<p><%= @task.title%></p>
<p><%= @task.details%></p>

<p><%= link_to "Edit",task_edit_path %></p>
<p><%= link_to "🗑",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure? You wish to delete #{@task.title}" } %></p>
<%= link_to "🔙",tasks_path %>
</div>
